<h1 class="display-5 mb-4">Cadastro de Projetos</h1>

<%= render "shared/errors", resource: @project %>

<%= render 'form' %>

<div>
  <%= link_to t('links.go_back'), :back, class: "btn btn-outline-primary mt-1 mb-1" %>
</div>
